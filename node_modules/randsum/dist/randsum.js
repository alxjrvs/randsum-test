"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.randsum = void 0;
var modifyRolls_1 = require("./modifyRolls");
var parseArguments_1 = require("./parseArguments");
var utils_1 = require("./utils");
function randsum(primeArgument, randsumOptions) {
    var _a = (0, parseArguments_1.parseArguments)(primeArgument, randsumOptions), detailed = _a.detailed, _b = _a.randomizer, randomizer = _b === void 0 ? utils_1.defaultRandomizer : _b, rollParameters = __rest(_a, ["detailed", "randomizer"]);
    var rollOne = function () { return randomizer(rollParameters.sides); };
    var initialRolls = __spreadArray([], new Array(rollParameters.quantity), true).map(function () { return rollOne(); });
    var result = (0, modifyRolls_1.modifyRolls)(initialRolls, rollParameters, rollOne);
    return detailed === true ? result : result.total;
}
exports.randsum = randsum;
//# sourceMappingURL=randsum.js.map