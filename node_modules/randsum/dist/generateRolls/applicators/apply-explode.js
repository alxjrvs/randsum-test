"use strict";
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.applyExplode = void 0;
function applyExplode(rollTotals, _a, rollOne) {
    var sides = _a.sides;
    var explodeCount = rollTotals.filter(function (roll) { return roll === sides; });
    var explodeResults = __spreadArray([], new Array(explodeCount), true).map(function () { return rollOne(); });
    return __spreadArray(__spreadArray([], rollTotals, true), explodeResults, true);
}
exports.applyExplode = applyExplode;
//# sourceMappingURL=apply-explode.js.map