"use strict";
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.findMatches = void 0;
var utils_1 = require("../utils");
function findMatches(notations) {
    var _a;
    var m;
    var matches = [];
    while ((m = utils_1.completeRollPattern.exec(notations)) !== null) {
        if (m.groups !== null && m.groups !== undefined) {
            for (var _i = 0, _b = Object.keys(m.groups); _i < _b.length; _i++) {
                var key = _b[_i];
                if (m.groups[key] !== undefined) {
                    matches = __spreadArray(__spreadArray([], matches, true), [(_a = {}, _a[key] = m.groups[key], _a)], false);
                }
            }
        }
    }
    return matches;
}
exports.findMatches = findMatches;
//# sourceMappingURL=find-matches.js.map