import { UniqueModifier } from '../..';
import { RollParameters } from '../../types';
export declare function applyUnique(rolls: number[], { unique, quantity, sides }: Pick<RollParameters, 'quantity' | 'sides'> & UniqueModifier, rollOne: () => number): number[];
